// Generated by Skyramp v1.3.40 on 2026-08-27 14:34:28.641588999 +0000 UTC m=+947.466905533
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output secret_sharing_access_history_drawer.spec.ts \
// 		--output-dir /home/runner/work/infisical/infisical/frontend/tests \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/infisical/infisical/.skyramp/secret_sharing_access_history_drawer_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:8080");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1408000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/login`);
    // Wait for network activities to finish
    await page.waitForTimeout(3000);


    const playwrightRequest0: Record<string, any> = {
        "email": "admin@test.com",
        "password": "Password123!"
    };

    await page.locator("#email").fill(getValue(playwrightRequest0, "email"));
    await page.locator("#current-password").fill(getValue(playwrightRequest0, "password"));
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/api/v3/auth/login**");
    await page.getByRole("button", { name: "Continue with Email" }).click();
    const response0 = await responsePromise0;
    expect(response0.status(), 'login status code').toBe(200);

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    const orgId = page.url().match(/organizations\/([^/?]+)/)![1];
    await page.goto(`${baseUrl}/organizations/${orgId}/secret-sharing?selectedTab=share-secret`);
    await page.waitForLoadState('networkidle');
    await page.getByRole("button", { name: "Share Secret" }).waitFor({ state: 'visible', timeout: 10000 });

    await page.getByRole("button", { name: "Share Secret" }).click();
    await page.getByRole("textbox", { name: "API Key" }).fill("access-history-check");
    await page.getByRole("textbox", { name: "Enter sensitive data to share" }).fill("db-password-rotate-me");
    await page.getByRole("button", { name: "Advanced Settings" }).click();
    await page.locator("#notify-on-access").click();
    await page.getByRole("button", { name: "Create Secret Link" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await page.getByRole("button", { name: "Close" }).click();
    const newestShareRow = page.getByRole("row").filter({ hasText: "access-history-check" }).first();
    await expect(newestShareRow).toContainText("access-history-check");
    await expect(newestShareRow).toContainText("Active");
    await newestShareRow.getByRole("button", { name: "actions" }).click();
    await page.getByRole("menuitem", { name: "Access history" }).click();
    await expect(page.getByText("Every attempt to open this")).toContainText("Every attempt to open this link, newest first. 0 total.");
    await expect(page.getByText("Not opened yet")).toBeVisible();
    await expect(page.getByText("Attempts to open this link will appear here")).toBeVisible();
});
