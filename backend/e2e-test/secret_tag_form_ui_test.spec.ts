// Generated by Skyramp v1.3.38 on 2026-08-19 14:19:59.606067898 +0000 UTC m=+1269.525920109
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output secret_tag_form_ui_test.spec.ts \
// 		--output-dir /home/runner/work/infisical/infisical/backend/e2e-test \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/infisical/infisical/.skyramp/secret_tag_form_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:8080");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1559000);

    const errors: Error[] = [];
    page.on('pageerror', (err) => errors.push(err));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/login`);
    // Wait for network activities to finish
    await page.waitForTimeout(15000);


    const playwrightRequest0: Record<string, any> = {
        "email": "admin@test.com",
        "password": "Password123!"
    };

    await page.locator("#email").fill(getValue(playwrightRequest0, "email"));
    await page.locator("#current-password").fill(getValue(playwrightRequest0, "password"));
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/api/v3/auth/login**");
    await page.getByRole("button", { name: "Continue with Email" }).click();
    const response0 = await responsePromise0;
    expect(response0.status()).toBe(200);

    // Wait for network activities to finish
    await page.waitForTimeout(20000);

    await page.getByRole("button", { name: "Add New Project" }).click();
    await page.getByRole("textbox", { name: "Type your project name" }).fill("Test Secret Project");
    await page.getByRole("button", { name: "Secrets Management" }).click();
    await page.getByRole("button", { name: "Create Project" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(8139);

    await page.getByText("Add a New Secret").click();
    await page.getByRole("textbox", { name: "Type your secret name" }).fill("MY_TEST_SECRET");
    await page.getByRole("textbox", { name: "secret value" }).fill("test-value-123");
    await page.getByRole("button", { name: "Create Secret" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(17790);

    await page.getByRole("button", { name: "Settings" }).click();
    await page.getByRole("link", { exact: true, name: "Secrets Management" }).click();
    await page.getByRole("button", { name: "Create Tag" }).click();
    await page.getByRole("textbox", { name: "Type your tag slug" }).fill("test-tag");
    await page.getByRole("button", { name: "Create" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(7040);

    await page.getByRole("link", { name: "Test Secret Project Secrets" }).click();
    await page.getByRole("row", { name: "MY_TEST_SECRET" }).hover();
    // Wait for network activities to finish
    await page.waitForTimeout(120000);

    await page.mouse.move(960, 362);
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.locator("body").click({ position: {"x": 1031, "y": 362} });
    await page.locator(".absolute > button:nth-child(4) > .inline-flex").click();
    await page.locator("#react-select-3-input").click();
    await page.waitForLoadState('networkidle');
    await page.getByRole("option", { name: "test-tag" }).waitFor({ state: 'visible', timeout: 10000 });
    await page.getByRole("option", { name: "test-tag" }).click();
    await page.getByRole("button", { name: "Save Tags" }).click();
    await expect(page.getByRole("button", { name: "Save Tags" })).not.toBeVisible();
    expect(errors).toHaveLength(0);
});
