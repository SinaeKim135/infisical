// Generated by Skyramp v1.3.38 on 2026-08-19 14:07:26.618081812 +0000 UTC m=+516.537934013
// Command: skyramp generate contract rest http://localhost:8080/api/v4/secrets/{secretName}/tags \
// 		--auth-type Bearer \
// 		--framework playwright \
// 		--language typescript \
// 		--method GET \
// 		--output-dir /home/runner/work/infisical/infisical/backend/e2e-test \
// 		--overwrite \
// 		--path-params secretName=MY_SECRET \
// 		--provider \
// 		--query-params projectId=a8746afa-d91b-4f04-8373-bda1dd9e3f6d,environment=dev,secretPath=/ \
// 		--response-data {"secretName":"MY_SECRET","tags":[]} \
// 		--response-status-code 200 \
// 		--runtime local

// Import of required libraries
import { test, expect, SkyrampClient, checkSchema, getBaseUrl, getValue, getResponseValue } from '@skyramp/skyramp';

// URL for test requests
const URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:8080");

// contract test for /api/v4/secrets/{secretName}/tags GET
test('testTagsGet', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Declaration of variables
    const secretName = "MY_SECRET";

    // Expected Response Body
    const expectedTagsGetResponseBody: string = `{
        "secretName": "MY_SECRET",
        "tags": []
    }`;

    // Execute Request
    const tagsGetResponse = await client.sendRequest({
        url: URL,
        path: "/api/v4/secrets/{secretName}/tags",
        method: "GET",
        headers: headers,
        pathParams: {"secretName": secretName},
        queryParams: {
            "environment": "dev",
            "projectId": "a8746afa-d91b-4f04-8373-bda1dd9e3f6d",
            "secretPath": "/"
        }
    });

    // Generated Assertions
    expect(tagsGetResponse.statusCode, 'status code').toBe(200);
    expect(checkSchema(tagsGetResponse, expectedTagsGetResponseBody), 'schema').toBeTruthy();
    expect(getResponseValue(tagsGetResponse, "secretName"), 'secretName').toBe("MY_SECRET");
    expect(getResponseValue(tagsGetResponse, "tags"), 'tags').not.toBeNull();
    expect(getValue(tagsGetResponse, "tags").length).toBe(0);
    expect(getValue(tagsGetResponse, "tags.0")).toBeUndefined();
});
