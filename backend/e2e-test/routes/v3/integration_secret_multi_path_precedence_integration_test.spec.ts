// Generated by Skyramp v1.3.40 on 2026-08-27 15:16:05.490822647 +0000 UTC m=+558.460879721
// Command: skyramp generate integration rest \
// 		--auth-type Bearer \
// 		--framework playwright \
// 		--language typescript \
// 		--output integration_secret_multi_path_precedence_integration_test.spec.ts \
// 		--output-dir /home/runner/work/infisical/infisical/backend/e2e-test/routes/v3 \
// 		--overwrite \
// 		--runtime local \
// 		--trace /home/runner/work/infisical/infisical/backend/e2e-test/routes/v3/scenario_integration-secret-multi-path-precedence.json

// Import of required libraries
import { test, expect, SkyrampClient, getBaseUrl, getValue } from '@skyramp/skyramp';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:8080");

test('testIntegration', async () => {
    test.setTimeout(60000);

    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Declaration of variables
    const secretPaths = "/p1,/p2,/p3,/p4,/p5,/p6,/p7,/p8,/p9,/p10,/p11";

    // Execute Request
    const rawGetResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/api/v3/secrets/raw",
        method: "GET",
        headers: headers,
        queryParams: {
            "recursive": ["true"],
            "secretPaths": ["/base,/app"]
        }
    });

    // Generated Assertions
    expect(rawGetResponse.statusCode, 'status code').toBe(400);
    expect(getValue(rawGetResponse, "statusCode")).toBe(400);
    expect(getValue(rawGetResponse, "message")).not.toBeNull();

    // Execute Request
    const rawGet1Response = await client.sendRequest({
        url: URL_localhost,
        path: "/api/v3/secrets/raw",
        method: "GET",
        headers: headers,
        queryParams: {"secretPaths": ["/p1,/p2,/p3,/p4,/p5,/p6,/p7,/p8,/p9,/p10,/p11"]}
    });

    // Generated Assertions
    expect(rawGet1Response.statusCode, 'status code').toBe(400);
    expect(getValue(rawGet1Response, "statusCode")).toBe(400);
    expect(getValue(rawGet1Response, "message")).not.toBeNull();
});
